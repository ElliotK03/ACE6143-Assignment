{
  "name": "ace6143-assignment",
  "version": "1.0.0",
  "description": "UDP Client and Server Assignment",
  "main": "index.js",
  "scripts": {
    "start:server": "cd udp-server && node index.js",
    "start:client": "cd udp-client && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ElliotK03",
  "license": "ISC",
  "workspaces": [
    "udp-client",
    "udp-server"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
